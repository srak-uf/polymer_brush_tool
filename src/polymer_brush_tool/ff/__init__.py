"""Force-field preparation sub-package.

Modules
-------
fragments
    Write AMBER ``prepgen`` chain files and tleap scripts.
minimize
    Run AMBER ``sander`` energy minimisation with pull constraints.
topology
    Patch GROMACS topology files (TIP3P include, position restraints).
"""
